# Я сделал агента, который кодит пока я сплю. И он врёт.

> ИИ не заменяет людей, люди просто больше работают. Так давайте хотя бы ночью пусть работает ИИ.

---

У меня 10+ проектов. Все через Claude Code. Подписка за 90 евро в месяц. И каждый вечер одно и то же: закрываю ноутбук, подписка простаивает. Утром открываю, контекст сброшен, объясняю заново.

Я подумал: а что если написать задачи перед сном, нажать Start и утром проснуться с результатом? Не просто "агент что-то написал", а с реальной проверкой. Потому что, как выяснилось, агент может написать `"status": "COMPLETED"` в checkpoint, а тесты при этом не проходят. Файлы, которые он "создал", не существуют на диске.

Мы поймали агента на лжи. И потратили неделю на то, чтобы научить его не врать.

Так появился **PocketCoder-A1**: автономный coding agent с верификацией, веб-дашбордом и поддержкой нескольких провайдеров. 7086 строк Python, 15 модулей, ни одного фреймворка.

---

## Содержание

1. [Зачем это вообще нужно](#1-зачем-это-вообще-нужно)
2. [Что такое PocketCoder-A1](#2-что-такое-pocketcoder-a1)
3. [Архитектура](#3-архитектура)
4. [Кейс: epotos-templates](#4-кейс-epotos-templates)
5. [Dashboard: 8 страниц без фреймворков](#5-dashboard)
6. [Выводы и что дальше](#6-выводы-и-что-дальше)

---

## 1. Зачем это вообще нужно

Знаете это чувство, когда ИИ-инструменты не заменяют вам работу, а добавляют её? Я начал делать вещи эффективнее и проектов стало больше. Сейчас их 10+. Все code-based. Все через Claude Code.

Проблема простая. Я работаю над одним проектом, а в голове крутятся мысли по другому: "надо добавить провайдер DeepSeek", "переписать конфиг", "написать тесты". Записать куда-то записал. Но руки дойдут через неделю. А подписка за 90 евро тикает.

Я попробовал OpenClaw. Не сработало, даже с хорошей моделью у меня вообще ничего не вышло. Может криво подключил, может что-то ещё, но результат нулевой.

Тогда я решил написать своё. Идея простая: написал задачи, ушёл гулять или спать, вернулся к результату. Агент работает сессиями, сохраняет состояние, берёт следующую задачу сам. После каждой сессии запускается реальная проверка: pytest, py_compile, проверка файлов на диске. Веб-дашборд показывает что происходит в реальном времени. Основной провайдер Claude Max, плюс Claude API и Ollama как экспериментальные.

Сделан прежде всего для себя. Я реально буду этим пользоваться. Но выложил в open source, вдруг кому-то тоже пригодится.

---

## 2. Что такое PocketCoder-A1

CLI + веб-дашборд. Установка:

```bash
git clone https://github.com/Chashchin-Dmitry/pocketcoder-a1.git
cd pocketcoder-a1
pip install -e .
```

Инициализация на любом проекте:

```bash
pca init /path/to/your-project
pca ui -d /path/to/your-project
```

Открывается дашборд на `http://localhost:7331`. Добавляешь задачи, нажимаешь Start Agent, агент работает автономно.

**Требования:** Python 3.10+, [Claude Code CLI](https://docs.anthropic.com/en/docs/claude-code) (`npm i -g @anthropic-ai/claude-code`).

| Параметр | Значение |
|----------|----------|
| Строк кода | 7086 |
| Модулей | 15 |
| Страниц дашборда | 8 |
| API endpoints | 24 |
| Провайдеров | 3 |
| E2E тестов | 6/6 |

Отличие от PocketCoder v1 (моего [предыдущего проекта](https://habr.com/ru/articles/991022/)): v1 это CLI-агент для локальных моделей, интерактивный. A1 это автономный менеджер задач + исполнитель. Нажал кнопку, ушёл. Совсем другой продукт.

---

## 3. Архитектура

Весь проект это 15 Python-модулей. Ни одного фреймворка. HTTP-сервер на стандартном `http.server`, JSON это стандартный `json`, файлы через `pathlib`. Единственные внешние зависимости это `playwright` для E2E тестов и опционально `anthropic` / `ollama` для альтернативных провайдеров.

### 3.1 Общая схема

Вот как работает система целиком, от `pca start` до завершения:

![Полный цикл агента](1.png)

Агент запускается, захватывает baseline (снимок валидации ДО работы), собирает промпт, работает через Claude CLI, парсит результат в реальном времени, верифицирует. Если всё ок, принимает результат. Если нет, инжектит ошибки в следующую сессию и пытается снова. Разберём каждый кусок.

### 3.2 Claude CLI как subprocess

Ядро системы это запуск Claude CLI как subprocess. Звучит просто, но в этой части скрывались самые коварные проблемы.

![Claude CLI subprocess: флаги, парсинг, callback](2.png)

Финальный вариант запуска:

```python
import os, subprocess

env = os.environ.copy()
env.pop("CLAUDECODE", None)  # без этого nested sessions падают

proc = subprocess.Popen(
    ["claude", "-p", prompt,
     "--dangerously-skip-permissions",
     "--no-session-persistence",
     "--max-turns", "25",
     "--verbose",
     "--output-format", "stream-json"],
    cwd=str(project_dir),
    env=env,
    stdout=subprocess.PIPE,
    stderr=subprocess.STDOUT,
    text=True,
    bufsize=1,
)
```

Почему именно так? Claude Code ставит переменную `CLAUDECODE=1` в окружении. Subprocess наследует это, внутренний `claude` видит переменную и падает: "cannot launch inside another session". Фикс одна строка `env.pop`. Найти причину 2 часа, потому что ошибка не попадала в stdout.

Другая недокументированная особенность: `--output-format stream-json` без `--verbose` просто молчит. Тишина. Ещё 2 часа на поиск.

| Флаг | Зачем |
|------|-------|
| `-p prompt` | Неинтерактивный режим |
| `--dangerously-skip-permissions` | Автоподтверждение всех tool_use |
| `--no-session-persistence` | Не засорять историю сессий |
| `--max-turns 25` | Защита от бесконечной работы |
| `--verbose` | Обязательно с stream-json + -p |
| `--output-format stream-json` | NDJSON поток вместо буферизации |

С `stream-json` Claude выдаёт NDJSON, по одному JSON-объекту на строку:

```json
{"type":"assistant","message":{"content":[{"type":"tool_use","name":"Read","input":{"file_path":"/src/app.py"}}]}}
{"type":"rate_limit_event","usage":{"input_tokens":12400,"output_tokens":3200}}
{"type":"result","result":"Task completed successfully."}
```

Парсер классифицирует каждое событие и отправляет в дашборд. `tool_use` с именем `Read` становится типом "read" (синий), `Bash` это "bash" (фиолетовый), `thinking` это жёлтый. Всего 8 типов иконок.

### 3.3 Верификация: "Не верим на слово"

Самая важная часть системы.

![Трёхуровневая верификация](4.png)

Когда агент говорит "COMPLETED", мы не верим. Запускается трёхуровневая проверка.

**Уровень 1, BLOCKING.** Если хоть одна проверка не прошла, сессия не принимается: `py_compile` на все .py файлы, `pytest`, проверка что файлы реально на диске, проверка `success_criteria` из задачи.

**Уровень 2, WARNING.** Результат только логируется: `ruff`, `build`, `git diff`.

**Уровень 3, ANTI-LOOP.** Защита от бесконечного цикла. Перед первой сессией делаем baseline, снимок всех существующих проблем. Старые баги не считаются. Если агент не может починить проблему за 5 попыток, задача помечается BLOCKED, агент переходит к следующей. При retry ошибки инжектятся прямо в промпт: "VERIFICATION FAILED, attempt 2/5, BLOCKING: tests 2 failed".

### 3.4 Провайдеры

![Три провайдера, общий pipeline](6.png)

| Провайдер | Как работает | Требует |
|-----------|-------------|---------|
| claude-max | CLI subprocess, Stream-JSON, все нативные инструменты | Claude Max подписка |
| claude-api | Anthropic SDK, agentic loop, 6 инструментов | API ключ |
| ollama | Текстовый стриминг, без tool calling | Локальный Ollama |

Claude Max основной. Claude API и Ollama это **EXPERIMENTAL**. Кто хочет помочь, welcome GitHub PRs. Я один, физически не успеваю всё довести. Сделал для себя, если кто-то хочет адаптировать, с радостью приму.

### 3.5 Real-time поток данных

![Поток данных: агент, дашборд, браузер](3.png)

Claude CLI выдаёт NDJSON, парсер в `loop.py` кладёт в `AGENT_LOG_BUFFER`, AJAX `/api/log` забирает каждые 2 секунды, браузер рисует. Метрики идут параллельно: `rate_limit_event` обновляет `_session_metrics`, `/api/status` отдаёт их каждые 3 секунды в 6 карточек. Таймер тикает через JavaScript каждую секунду.

### 3.6 Жизненный цикл задачи

![Задачи: создание, приоритеты, статусы](5.png)

Три способа создать задачу. CLI через `pca task add`. Форма Quick Add на дашборде. Или AI Transform, когда пишешь сырой текст и ИИ разбивает его на структурированные задачи с приоритетами и критериями. Приоритеты меняются через drag-and-drop. Состояния: PENDING, IN PROGRESS, DONE или BLOCKED.

---

## 4. Кейс: epotos-templates

Реальный кейс. epotos-templates это проект для компании, обработка документов + генерация шаблонов. Нужно было добавить переключение провайдеров (с Ollama на DeepSeek). Я записал мысли в свободной форме и запустил A1.

### Шаг 1: Пустая страница, 5 задач за 30 секунд

Открываю дашборд. Страница Tasks пустая:

![Пустая страница задач](01_tasks_empty.png)

Вместо того чтобы вручную формулировать задачи, использую AI Transform. Просто пишу сырой текст: "хочу клонировать epotos-templates, посмотреть как настроены провайдеры, добавить блок переключения с llama на deepseek..."

![Ввод текста в AI Transform](02_transform_input.png)

Нажимаю Transform:

![Обработка](03_transform_processing.png)

Через несколько секунд 5 структурированных задач с описаниями и критериями успеха:

![Предпросмотр 5 задач](04_transform_preview.png)

Нажимаю "Add Selected", задачи созданы с приоритетами #1-#5:

![5 задач созданы](05_tasks_5_pending.png)

### Шаг 2: Запуск

Dashboard. 0/5 задач, IDLE. Нажимаю Start Agent:

![Dashboard перед стартом](06_dashboard_idle.png)

Детали первой задачи до начала работы:

![Детали задачи](07_task_detail_empty.png)

Настройки, выбираю claude-api, ввожу API ключ:

![Настройки провайдера](09_settings_claude_api.png)

### Шаг 3: Агент работает

В логе появляются строки в реальном времени. THINK, BASH, READ:

![Агент работает, live log](10_dashboard_running_log.png)

Task Detail: 11 tool calls, лог заполняется:

![Детали задачи, агент работает](11_task_detail_live.png)

### Шаг 4: Первый результат

1/5 задач выполнена. Зелёная галочка. Агент автоматически перешёл к следующей:

![1 задача выполнена](12_dashboard_1_done.png)

Агент анализирует кодовую базу, читает ollama.ts, ai-client.ts, грепает localhost:11434:

![Анализ кодовой базы](13_dashboard_log_task015.png)

### Шаг 5: Прогресс

2/5 задач выполнены, task_016 in progress:

![2 выполнены, 1 в работе](14_tasks_2done_3pending.png)

![Чистый вид](15_tasks_2done_clean.png)

Агент проектирует архитектуру провайдеров:

![Проектирование архитектуры](16_dashboard_log_task016.png)

### Шаг 6: Сообщение агенту

Пока агент работает, печатаю в форму "Message to Agent", хочу чтобы он ещё и документацию написал:

![Набираю сообщение](17_dashboard_message_typed.png)

Сообщение в очереди, агент прочитает на следующей сессии:

![Сообщение отправлено](18_dashboard_message_sent.png)

### Шаг 7: Всё готово

5/5 задач! Агент ещё работает, обрабатывает моё сообщение про документацию:

![5/5 задач, агент ещё работает](19_dashboard_5of5_running.png)

Верификация пройдена, агент пишет документацию:

![Верификация + документация](20_livelog_verification_docs.png)

Обновляет CLAUDE.md, создаёт MEMORY.md:

![Пишет документацию](21_livelog_memory_write.png)

### Финал

COMPLETED. 5/5 задач, 12 сессий, 23 файла изменено:

![COMPLETED](22_dashboard_completed.png)

### Светлая тема

Дашборд работает в обеих темах:

![Задачи, светлая](23_tasks_light.png)

![Dashboard, светлая](24_dashboard_light.png)

![Лог, светлая](25_dashboard_log_light.png)

![Детали задачи, светлая](26_task_detail_done_light.png)

---

## 5. Dashboard

3491 строка чистого Python. Ни React, ни Vue, ни Flask. Стандартный `http.server` + `string.Template`. Весь CSS, JavaScript и HTML в одном файле.

### 8 страниц

| Страница | URL | Что делает |
|----------|-----|------------|
| Dashboard | `/` | 6 карточек метрик, Start/Stop, live log, Quick Add |
| Tasks | `/tasks` | Список + drag-and-drop + bulk add + AI Transform |
| Task Detail | `/task/{id}` | Полный вид + логи + метрики + Start/Stop/Delete |
| Sessions | `/sessions` | История сессий с метриками |
| Activity Log | `/log` | Хронология действий |
| Settings | `/settings` | Провайдер, API ключ, параметры |
| Commits | `/commits` | Git история с иконками типов |
| Transform | `/transform` | Сырой текст, AI, задачи |

### 6 карточек метрик

| Карточка | Данные |
|----------|--------|
| Tasks | `2/5 done` + прогресс-бар |
| Session | `#3` + WORKING/IDLE/COMPLETED |
| Tokens | `12.4K in / 3.2K out` + прогресс контекста |
| Cost | `$0.08` за сессию |
| Duration | `48s` (live timer, тикает каждую секунду) |
| Files | `3 modified` |

Лог стилизован под macOS Terminal. Тёмный фон, моноширинный шрифт, 8 типов иконок (read/edit/write/bash/thinking/text/metric/verify). Обновляется через AJAX каждые 2 секунды.

Полный REST API на 24 endpoints. Можно мониторить из скриптов:

```bash
curl http://localhost:7331/api/status | python -m json.tool
curl "http://localhost:7331/api/log?since=0" | python -m json.tool
```

---

## 6. Выводы и что дальше

PocketCoder-A1 работает. 5 задач на реальном проекте за 13 минут автономно. С верификацией, с live-логом, с возможностью отправить сообщение агенту прямо во время работы.

Автономные сессии работают: нажал Start, ушёл, вернулся к результату. Верификация реально ловит врущего агента на трёх уровнях: blocking, warning, anti-loop. Dashboard показывает всё в реальном времени. Queue Message позволяет писать агенту пока он работает.

В разработке: auto-checkpoint при 70% контекста, git integration (ветки + atomic commits), PyPI + uv release.

Сейчас бум вайб-кодинга. Прикольно было бы больше тулзов для оптимизации работы. Не замена человеку, расширение возможностей. Ты спишь, а подписка работает.

---

**PocketCoder-A1** это про open source.

**GitHub:** [github.com/Chashchin-Dmitry/pocketcoder-a1](https://github.com/Chashchin-Dmitry/pocketcoder-a1)

---

Буду рад за лайк и подписку на канал :)
[https://t.me/notes_from_cto](https://t.me/notes_from_cto)

Наш сайт: [https://bvmax.ru/ai](https://bvmax.ru/ai)
